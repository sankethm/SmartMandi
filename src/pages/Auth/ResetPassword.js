import React from 'react';
const ResetPassword = () => (
  <div style={{maxWidth:520, margin:'40px auto', padding:24, background:'#fff', borderRadius:8}}>
    <h3>Reset Password (mock)</h3>
    <p>Reset form placeholder.</p>
  </div>
);
export default ResetPassword;